# AWS Lambda Requirements for Contract Analyzer
# Keep this minimal to reduce cold start time

# AI Services
openai==1.12.0                # OpenAI GPT-4 (primary AI service)
google-generativeai==0.3.2    # Google Gemini (fallback AI service)

# AWS SDK (usually pre-installed in Lambda, but explicit for local testing)
boto3==1.34.34                # AWS S3, DynamoDB access

# JSON handling and utilities
python-dateutil==2.8.2        # Date/time utilities
